import styled from "styled-components";
import { Container, Jumbotron } from 'react-bootstrap';

export const NotFoundedContainer = styled(Container)`
  justify-content: center;
  position: relative;
  min-height: 100vh;
  text-align: center;
`;
export const NotFoundedJumbotron = styled(Jumbotron)`
  background: #6A1EDF;
  color: #f3f3f3;
  text-align: center;
`;
