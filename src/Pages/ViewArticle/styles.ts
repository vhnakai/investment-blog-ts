import styled from "styled-components";
import { Container, Row, Col, Jumbotron } from "react-bootstrap";

export const ShowContainer = styled(Container)`
  justify-content: center;
  position: relative;
  min-height: 100vh;
`;
export const ShowRow = styled(Row)``;
export const ShowCol = styled(Col)``;
export const ShowJumbotron = styled(Jumbotron)`
  background: #6A1EDF;
  color: #f3f3f3;
  text-align: center;
`;


